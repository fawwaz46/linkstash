# Leave these unset for local development — the app uses a local SQLite file
# (file:linkstash.db) automatically.
#
# To deploy on serverless (e.g. Vercel), create a free libSQL database at
# https://turso.tech and set:
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-token
